apache_mods:
  # Modules to be enabled in Apache
  - rewrite
  - wsgi
  - php7.0

vhost:
  servername: "webserver.example.com"
  serveralias: "webserver"
  serveradmin: "admin@example.com"
  documentroot: "/var/www/html"
